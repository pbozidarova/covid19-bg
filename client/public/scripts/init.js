import * as sliders from './sliders.js';
import * as buildLabels from './buildLables.js'


const notifications = {
    fetchFromOpenDBError: "Данните от https://data.egov.bg/ в момента са недостъпни."
}
